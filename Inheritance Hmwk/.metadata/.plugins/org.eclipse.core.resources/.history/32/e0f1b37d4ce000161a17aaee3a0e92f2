package inheritance;

public class Card {

	protected String name; 
	
	/**
	 * Will construct the object if no string is given, and set the name to john.
	 *
	 * @param  nothing
	 * @return nothing
	 */
	
    public Card() 
    { 
        this("john"); //calling the other constructor using 
                      //  the keyword ‘this’ 
    } 
    
	/**
	 * Will construct the object if a string is given, and set the name to the string.
	 *
	 * @param  string
	 * @return nothing
	 */
    
    public Card(String n) 
    { 
        name = n; 
    } 

    public String getName() 
    { 
        return name; 
    } 

    public boolean isExpired() 
    { 
        return false; 
    } 

    public String format() 
    { 
        return "Card holder: " + name; 
    } 
    
    public String toString()
    {
    	return this.getClass().getSimpleName() + "[name=" + name + "]";
    }
    
    public boolean equals(Card card1) {
    	if (this.toString().equals(card1.toString())) {
    		return true;
    	} else {
    		return false;
    	}
    }
}
